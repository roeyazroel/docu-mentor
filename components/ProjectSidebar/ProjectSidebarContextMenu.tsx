import React from "react";

/**
 * Stub for ProjectSidebarContextMenu. Replace with actual context menu logic.
 */
export function ProjectSidebarContextMenu({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
